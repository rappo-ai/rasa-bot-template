version: "2.0"

intents:
  - help
  - helpg

actions:
  - action_session_start
  - action_echo

slots:
  chat_type:
    type: categorical
    values:
      - private
      - group
      - channel

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
